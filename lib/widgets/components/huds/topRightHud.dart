import 'package:flutter/material.dart';

class TopRightHud extends StatelessWidget {
  const TopRightHud({super.key});

  @override
  Widget build(BuildContext context) {
    return FilledButton.tonal(
      onPressed: () {},
      child: Text("Create Assessment"),
    );
  }
}

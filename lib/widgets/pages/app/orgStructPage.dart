import 'package:flutter/material.dart';

class OrgStructPage extends StatelessWidget {
  const OrgStructPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}